import { Controller } from '@hexajs/core';

/**
 * Background controller for the "hexa-grayscale" namespace.
 * Add @Action methods here to handle messages from content scripts and the popup.
 */
@Controller({ namespace: 'hexa-grayscale' })
export class HexaGrayscaleController {
	// @Action('example')
	// onExample(payload: unknown): unknown {
	//   return payload;
	// }
}
