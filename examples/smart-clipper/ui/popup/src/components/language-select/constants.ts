export const LANGUAGE_META_TEXT = {
  bundled: 'bundled',
  downloadOnFirstUse: 'download on first use'
} as const;

export const LANGUAGE_NOTE_TEXT = {
  ready: 'is ready locally.',
  missingModels: 'will download missing models on first use, then reuse the cached files later.'
} as const;
