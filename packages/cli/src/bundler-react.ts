/**
 * @deprecated Re-exported for backwards compatibility. Prefer
 * `loadFrameworkPlugin(cwd, framework)` from './bundler-framework'.
 */
export { loadReactPlugin, loadFrameworkPlugin } from './bundler-framework';
