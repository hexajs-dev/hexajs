/**
 * HexaJS Validation System
 * 
 * Pure decorator-based validation that is processed at compile time via AOT.
 * No runtime library required—decorators are metadata-only markers that guide
 * the CLI scanner to generate type-safe validation functions.
 */

export * from './decorators';
